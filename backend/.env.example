# ============================================
# CONFIGURATION ESIMEN / SHEK'S HOUSE
# ============================================

# Configuration de la base de données
# Railway fournit automatiquement ces variables:
# MYSQL_HOST, MYSQL_USER, MYSQL_PASSWORD, MYSQL_DATABASE
DB_HOST=localhost
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=esimen

# Configuration du serveur
PORT=3006
NODE_ENV=development

# Securite JWT (IMPORTANT: generer une cle secrete unique en production)
# Utilisez: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production

# Configuration Stripe
# Test: sk_test_... / Live: sk_live_...
STRIPE_SECRET_KEY=sk_test_your_stripe_key

# Configuration Email (Gmail App Password)
# Pour obtenir un App Password: https://myaccount.google.com/apppasswords
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# URLs (IMPORTANT: modifier en production)
BASE_URL=http://localhost:3006
FRONTEND_URL=http://localhost:3006

# CORS - Origines autorisees (separer par des virgules)
# En production: https://votre-site.netlify.app
ALLOWED_ORIGINS=http://localhost:3006,http://127.0.0.1:3006
